# WebBrain 本地版改造备忘（2026-08-15/16）

**Git 仓库（2026-08-16 起）**：`https://github.com/MarkLee0030/web_brain.git`（branch main）。
本地目录 D:\WebBrain 即仓库根（含 chrome-31.0.1 源码 + 本文件；zip 包在 .gitignore 中）。
推不上时的两个已知坑：
- GitHub push protection 会把 vendor/transformers/transformers.web.js 里的
  `Mistral3ForConditionalGeneration` 类名误判为 Mistral API Key——已用字符串拼接
  （`"Mistral3"+"ForConditionalGeneration"`）拆开，运行值不变。**以后换新版 vendor
  包时该字符串会回来，推送会再次被拦**，届时要么重新拼接，要么在 GitHub
  Security → secret scanning 里按提示 unblock 该 pattern。
- 同理该文件 JSDoc 注释里的 40 位 commit 哈希（@see 链接）也被误判过，已缩为 7 位短哈希。

当前运行版本：`D:\WebBrain\chrome-31.0.1`（Chrome 已加载已解压的扩展程序）

## 已做的自定义：助手人设（Persona）UI 化

**最终形态（2026-08-16）**：设置页 → Memory 标签 → "助手人设 / Assistant persona" 卡片，
textarea 编辑 + 保存/清除按钮，保存后**实时生效**（无需重载扩展）。

### 数据流

1. UI 文本框 → `chrome.storage.local` 的 `personaText` 键（settings.js）
2. background.js 启动时 `loadPersona()`：storage 为空(undefined)时用 `persona.js`
   文件内容做一次性种子；之后以 UI 为准（空字符串 = 用户主动清空，不再回填）
3. background.js `chrome.storage.onChanged` → `agent.personaText = ...` +
   `agent._refreshSystemPrompts()`（实时重建提示词）
4. agent.js `_buildSystemPrompt()` 在基础提示词之后追加 persona 块

### 修改文件清单（升级新版本后照此重打）

1. **新增** 根目录 `persona.js`（种子内容 + 兜底，导出 `PERSONA`）
2. `src/agent/agent.js`：
   - 构造函数 `this.profileText = '';` 后加 `this.personaText = '';`
   - `_buildSystemPrompt()` 内 dev-appendix 块后加：
     ```js
     if (this.personaText && this.personaText.trim()) {
       prompt += `\n\n${this.personaText.trim()}`;
     }
     ```
3. `src/background.js`：
   - 顶部 import：`import { PERSONA } from '../persona.js';`
   - `loadProfile();` 之后加 `loadPersona()` 函数（种子逻辑）+ 调用
   - onChanged 里 profileText 块后加 personaText 块（同步 + refreshPrompts）
4. `src/ui/settings.html`：user-memory-card 之前插入 persona-card 区块
   （id: persona-text / btn-save-persona / btn-clear-persona / test-persona）
5. `src/ui/settings.js`：元素引用 + 两处加载逻辑加 personaText + 保存/清除
   处理器 + flashPersonaResult 函数
6. `src/ui/locales/en.js` + `zh.js`：加 `st.persona.*` 文案键

### 验证方法

- 设置页改人设 → 保存 → **新对话**问"你叫什么名字" → 立即生效
- persona 在系统提示词中的位置：基础提示词之后、Skills 之前（非最开头，
  基础提示词在最前是为了吃提示词缓存）

## 已做的自定义：批量图片下载 Skill（2026-08-16）

新增打包技能 **Batch image download**（画廊批量下载图片到下载目录），默认种子启用。

### 修改文件清单（升级新版本后照此重打）

1. **新增** `skills/batch-image-download.md`（技能正文 + `webbrain-skill` JSON 元数据）
2. `src/agent/skills.js`：
   - `PACKAGED_SKILL_SOURCES` 末尾加 `batch-image-download` 条目（id/name/path）
   - `DEFAULT_SKILL_SOURCES` 的 includes 数组加 `'batch-image-download'`
3. `src/download-directory.js`：`normalizeDownloadDirectory()` 支持 Windows
   绝对路径（`D:/images`，盘符+斜杠正则分支），相对路径逻辑不变
4. `src/ui/locales/en.js` + `zh.js`：`st.display.download_directory.desc/error`
   文案改为允许绝对路径（目录需已存在；".." 仍禁止）
5. `src/agent/tools.js` 三处下载描述措辞对齐："浏览器 Downloads 文件夹" →
   "用户配置的下载目录（默认浏览器 Downloads）"：
   - `download_resource_from_page` description
   - `download_files` description（新增落地位置说明）
   - `download_social_media` description
6. `skills/freeskillz-xyz.md` 两处同款措辞对齐（`download_public_media`
   工具 description + Workflow 第 8 步）

### 用法

- 设置 → Display → Download directory 可填绝对路径（如 `D:\images`，文件夹需已存在），
  模型触发的下载会直接落进该目录（通过 `downloads.onDeterminingFilename` 路由，
  无需人工挪文件）。留空 = 系统默认下载目录。
- 对话中直接说"把这个页面里的图都下载下来"即可触发技能（Act 模式，Mid/Full 档）。

## 已做的自定义：新对话不再删历史（2026-08-16）

上游行为：点"新建对话"/`/reset` 会删除当前 tab 的聊天历史记录（历史页清空）。
已改为：只重置内存身份，旧记录保留在历史页，新对话另存为新记录。

### 修改文件清单（升级新版本后照此重打）

1. `src/ui/sidepanel.js`：
   - `resetChatHistoryStateForTab()` 中删除 hydrate-identity 块 + `deleteChatHistoryRecord`
     批量删除块（保留内存 map 清理与 saveSeq 失效）
   - 顶部 import 移除不再使用的 `deleteChatHistoryRecord`
   （历史页 history.js 自己的人工删除按钮不受影响）

## 已做的自定义：思考强度按钮（2026-08-16）

侧栏 header 新增紧凑按钮（语言选择器左侧），点击循环当前模型的思考强度档位，
只对 Muse-Glimmer 和 Qwen3.5+ 系模型显示（其他模型隐藏）。

### 档位规划（按模型已有档位）

- Muse-Glimmer：auto → low → medium → high → xhigh（`reasoning_strength`）
- Qwen3.5+（含 Qwen3.8）：auto → off → low → medium → xhigh
  （`reasoning_effort`；off = `enable_thinking:false`）
- auto = 不注入，跟随服务器默认（Muse bat high / agent bat xhigh；Qwen bat xhigh）

### 修改文件清单（升级新版本后照此重打）

1. `src/providers/provider-compatibility.js`：
   - `detectedCompatibilityPreset()` 加 `model.includes('muse'||'glimmer') → 'muse'`
   - `compatibilityRequestBody()`：qwen 分支按 `/qwen3[._-]?[5-9]/` 模型名分流——
     新版发 `chat_template_kwargs.reasoning_effort`（minimal→low, high/max→xhigh），
     旧版保持 enable_thinking 开关；新增 muse 分支发 `reasoning_strength`
     （off/minimal→low, max→xhigh）
   - `mergeProviderRequestBody()` 里 enable_thinking=false 时同步删 `reasoning_effort`
2. `src/ui/sidepanel.html`：header-right 里 language-picker 前加
   `#thinking-strength-btn`（provider-picker-btn thinking-strength-btn hidden，
   内含 `#thinking-strength-label`）
3. `src/ui/sidepanel.js`：
   - 元素引用 providerSelect 旁加 thinkingStrengthBtn/Label
   - `loadProviders()`：res 后存 `providersById` Map，末尾调 `syncThinkingStrengthButton()`
   - 新增整块：THINKING_LADDERS / THINKING_LEVEL_FALLBACK / thinkingFamilyForModel /
     normalizeThinkingLevel / syncThinkingStrengthButton / 点击 handler
     （点击 → update_provider 合并 compat.reasoningEffort, markConfigured:false → 刷新标签）
   - providerSelect change handler 成功路径（selectedProviderId = providerId 后）加
     `syncThinkingStrengthButton()`
4. `src/ui/locales/en.js` + `zh.js`：加 `sp.thinking.prefix` / `sp.thinking.title`

### 说明

- 按钮写的就是设置页 Providers → 兼容性里的 reasoningEffort 同一个键，
  两边数值保持同步；保存即生效（下一轮请求自动带上），无需重载扩展。
- planner 请求会强制关思考（qwen 系 enable_thinking=false 时已同步删掉
  reasoning_effort 防止打架）；muse 的 planner 用用户档位（关不掉思考）。

## 已做的自定义：去掉 "supported sites" 措辞（2026-08-16）

模型看到 "supported sites/supported list" 会多问一轮「这个网站是否支持」。
已清理相关措辞并显式加「不要预问，直接试，报告实际结果」规则：

1. `src/agent/tools.js`：
   - `download_social_media` description 开头去掉站点清单和 supported 表述，
     加 "Do not ask which site this is or whether the page is supported — try it
     and report the actual result."
   - SYSTEM_PROMPT 中 SMD recommendation 说明："a site outside SMD's supported
     list" → "a page with no downloadable media it can reach"
2. `skills/freeskillz-xyz.md`：summary/正文/tool description/allowlist 规则共 5 处
   去 "supported"，加「不预问，直接尝试并转达实际结果」
3. `skills/batch-image-download.md`：Rules 加「不预问网站/图片类型是否支持」

## 已做的自定义：工作目录沙箱（2026-08-16）

侧栏 header 新增「工作目录」按钮（语言按钮旁）：用户选一个本地文件夹，模型获得
6 个 `workspace_*` 工具，**只能在该文件夹内操作**（建/删文件夹、生成文件写入、
读回文本文件、批量下载直写目录）。

### 四层 guards

1. OS 级沙箱：FileSystemDirectoryHandle 根句柄无法逃逸目录树（`..` 由 API 拒绝）
2. 路径校验：`normalizeWorkspacePath()` 单点拒绝绝对路径/盘符/`..`/非法字符
3. 权限门：`Capability.FILESYSTEM` + 合成 host "the working directory"
   （clarify 卡：once/always/deny）；`workspace_download` 双能力
   [FILESYSTEM, DOWNLOAD]（同时按 URL 主机门）
4. 提示词边界：`_buildSystemPrompt()` 注入 `[WORKING DIRECTORY]` 块（仅在选了目录时）

### 修改文件清单（升级新版本后照此重打）

1. **新增** `src/workspace/workspace-fs.js`：路径校验、list/mkdir/delete/write_file/
   read_file/download 六个操作（download = fetch+createWritable 直写，uniquify 防覆盖）、
   IndexedDB 句柄持久化（库 `webbrain_workspace`）、loadWorkspaceHandle 水合
2. `src/agent/permission-gate.js`：`Capability.FILESYSTEM` + label + 三工具
   TOOL_CAPABILITY + `capabilitiesFor` 的 workspace_download 双能力 +
   `hostForCapability` FILESYSTEM 分支返回 "the working directory"
3. `src/agent/tools.js`：AGENT_TOOLS 加 6 个 workspace_* 定义（download_files 条目后）+
   `MID_TOOL_NAMES` 加 6 名（FULL 自动含；Compact 不含）+ SYSTEM_PROMPT_ACT 与
   ACT_MID 各加一段 WORKING DIRECTORY 说明
4. `src/agent/agent.js`：`import workspace-* from '../workspace/workspace-fs.js'`；
   executeTool 的 download_files 分支后加 6 个 dispatch 分支；构造函数加
   `this.workingDirectoryName = '';`；`_buildSystemPrompt()` persona 块后加
   `[WORKING DIRECTORY]` 注入
5. `src/background.js`：import workspace 函数；lightweightAction 加
   `get_working_directory_state`；三个消息 case（get/pick/clear_working_directory）；
   `loadWorkingDirectory()` 水合（loadPersona 后）；onChanged 加 workingDirectory 分支
6. `src/ui/sidepanel.html`：header-right 加 `#btn-working-dir`（thinking-strength-btn 前）
7. `src/ui/sidepanel.js`：引用 + 状态 + `syncWorkingDirButton()`/`loadWorkingDirState()` +
   点击 handler（showDirectoryPicker id=webbrain-working-dir → pick_working_directory）+
   storage.onChanged 的 workingDirectory 分支
8. `src/ui/locales/en.js` + `zh.js`：`sp.working_dir.title/needs_auth/none`

### 说明

- 句柄是 structured-cloneable 但非 JSON——只能存 IndexedDB（SW 侧），
  chrome.storage.local 只镜像 `{name}`；浏览器重启后 Chrome 收回 FSA 授权，
  按钮显示 ⚠，点击重选目录即重新授权。
- `workspace_download` 依赖 `<all_urls>` host 权限（manifest 已有），
  CDN 阻断时逐条报失败。写文件默认 uniquify（-N 后缀），`overwrite:true` 才覆盖。
- 与 chrome.downloads 的下载目录路由并存，互不影响。

## 其他已确认的机制（无需改动）

- 基础 system prompt 硬编码于 `src/agent/tools.js`（ASK/ACT/DEV 三套 + compact/mid 档）。
- Custom Skills（设置 → Skills）：planner 按 catalog 自动激活，Compact 档不加载，
  Mid/Full 档才加载；格式 = markdown + `webbrain-skill` JSON 元数据（summary/modes/intents）。
- Provider 的 Prompt tier 选 Full 才能完整吃到 skills + persona 上下文。
- 打包技能注册在 `src/agent/skills.js` 的 `PACKAGED_SKILL_SOURCES`；`loadCustomSkills`
  会按 id 去重补种（新技能重载扩展后自动出现在设置 → Skills）。
